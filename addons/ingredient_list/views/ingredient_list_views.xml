<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Ingredient List Tree View -->
        <record id="view_ingredient_list_tree" model="ir.ui.view">
            <field name="name">ingredient.list.tree</field>
            <field name="model">ingredient.list</field>
            <field name="arch" type="xml">
                <list string="Ingredient Lists" sample="1">
                    <field name="name"/>
                    <field name="source_recipe_ids" widget="many2many_tags"/>
                    <field name="ingredient_ids" widget="statinfo" string="Ingredients"/>
                </list>
            </field>
        </record>

        <!-- Ingredient List Form View -->
        <record id="view_ingredient_list_form" model="ir.ui.view">
            <field name="name">ingredient.list.form</field>
            <field name="model">ingredient.list</field>
            <field name="arch" type="xml">
                <form string="Ingredient List">
                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <field name="name" placeholder="Ingredient List Name"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="source_recipe_ids" widget="many2many_tags" options="{'color_field': 'id'}" string="Source Recipes"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Ingredients">
                                <field name="ingredient_ids">
                                    <list editable="bottom">
                                        <field name="sequence" widget="handle"/>
                                        <field name="name"/>
                                        <field name="quantity"/>
                                        <field name="unit"/>
                                        <field name="notes"/>
                                    </list>
                                </field>
                            </page>
                            <page string="Notes">
                                <field name="notes" placeholder="Additional notes..."/>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Ingredient List Search View -->
        <record id="view_ingredient_list_search" model="ir.ui.view">
            <field name="name">ingredient.list.search</field>
            <field name="model">ingredient.list</field>
            <field name="arch" type="xml">
                <search string="Search Ingredient Lists">
                    <field name="name"/>
                    <field name="source_recipe_ids"/>
                    <separator/>
                    <filter string="Group by Source Recipes" name="group_source_recipes" context="{'group_by': 'source_recipe_ids'}"/>
                </search>
            </field>
        </record>

        <!-- Ingredient List Wizard View -->
        <record id="view_ingredient_list_wizard_form" model="ir.ui.view">
            <field name="name">ingredient.list.wizard.form</field>
            <field name="model">ingredient.list.wizard</field>
            <field name="arch" type="xml">
                <form string="Create Ingredient List">
                    <sheet>
                        <group>
                            <group>
                                <field name="name"/>
                            </group>
                        </group>
                        <group>
                            <field name="recipe_id" invisible="1"/>
                            <field name="recipe_ids" widget="many2many_tags"/>
                        </group>
                    </sheet>
                    <footer>
                        <button string="Create Ingredient List" name="action_consolidate" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Ingredient List Action -->
        <record id="action_ingredient_list" model="ir.actions.act_window">
            <field name="name">Ingredient Lists</field>
            <field name="res_model">ingredient.list</field>
            <field name="view_mode">list,form</field>
            <field name="search_view_id" ref="view_ingredient_list_search"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first ingredient list!
                </p>
                <p>
                    Combine ingredients from multiple recipes into a single consolidated list.
                </p>
            </field>
        </record>

        <!-- Create Ingredient List Action -->
        <record id="action_create_ingredient_list_wizard" model="ir.actions.act_window">
            <field name="name">Create Ingredient List</field>
            <field name="res_model">ingredient.list.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

        <!-- Menu Items -->
        <menuitem id="menu_ingredient_list_root" name="Ingredient Lists" sequence="15"/>
        <menuitem id="menu_ingredient_list_lists" name="All Ingredient Lists" parent="menu_ingredient_list_root" action="action_ingredient_list" sequence="10"/>
        <menuitem id="menu_ingredient_list_create" name="Create Ingredient List" parent="menu_ingredient_list_root" action="action_create_ingredient_list_wizard" sequence="20"/>

    </data>
</odoo>

